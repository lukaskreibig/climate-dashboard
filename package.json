{
    "name": "climate-dashboard",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "concurrently \"python -m uvicorn backend.main:app --reload --port 8000\" \"cd frontend && yarn dev\""
    },
    "devDependencies": {
      "concurrently": "^7.0.0"
    }
  }
  